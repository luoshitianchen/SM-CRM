apiVersion: v1
kind: Service
metadata:
  name: sm-crm
  namespace: sm
  labels:
    app: sm-crm
    platform: sm-suite
spec:
  type: ClusterIP
  selector:
    app: sm-crm
  ports:
  - name: http
    port: 8510
    targetPort: http
    protocol: TCP
