apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: sm-crm
  namespace: sm-platform
  annotations:
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    nginx.ingress.kubernetes.io/proxy-body-size: "1m"
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - sm-crm.sm-platform.example.com
      secretName: sm-crm-tls
  rules:
    - host: sm-crm.sm-platform.example.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: sm-crm
                port:
                  number: 8300
